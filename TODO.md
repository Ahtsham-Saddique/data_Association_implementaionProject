# TODO

- [ ] Fix landing page to include a working Login button (“Please login, if you have already account”).
- [ ] Ensure the button links to `/login`.
- [ ] (Optional) Add conditional navigation so logged-in users don’t need to see the login CTA.
- [ ] Test: start server and verify redirect to `/login`.

